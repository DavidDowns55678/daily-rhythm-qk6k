const project = { name: "daily-rhythm-qk6k", owner: "DavidDowns55678", profile: "0040" };

export function describe() { return project.name + " ready for " + project.owner; }

console.log(describe());
